<% if @review.errors.any? %>
    <div id="errors_explanation" >
        <h2> <%= pluralize(@review.errors.count, "error") %> prohibited this restaurant from being saved: </h2>
        <ul>
            <% @review.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>

<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content, size: "60x12" %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.submit "Submit" %>
<% end %>